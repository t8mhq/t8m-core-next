# Pack: payments — bounded context (ADR-0001, core spec §1).
# Dependencies are the encoded graph (D4). Changing them is a visible
# decision recorded as a D-delta in the gate report that ships it.
enforce_dependencies: true
enforce_privacy: true
# "." is the shared kernel (DomainEvents.publish from settlement ingestion). Not part of
# the core-spec §1 inter-pack graph (check-pack-graph ignores it).
dependencies:
  - "."
